\documentclass[journal]{IEEEtran}

\usepackage{amsmath,amssymb,amsfonts,bm}
\usepackage{graphicx}
\usepackage{cite}
\usepackage{url}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{microtype}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{subcaption}
\usepackage[pagebackref=false,breaklinks=true,colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

% Math command shortcuts
\newcommand{\R}{\mathbb{R}}
\newcommand{\DTwoRO}{\text{D}^2\text{RO}}
\newcommand{\SWDGO}{\text{SW-DGO}}

\graphicspath{{figures/}}

\begin{document}

\title{Socially-Weighted Distributed Graph Optimization ($\text{D}^2\text{RO}$) for Autonomous Multi-Agent Service Fleets in Crowded Environments}

\author{Polla~Fattah,~\IEEEmembership{Senior Member,~IEEE,}
        and~Research~Collaborators%
\thanks{P. Fattah is with the Department of Computer Science and Robotics, Faculty of Engineering and Applied Sciences. (e-mail: polla.fattah@research.org).}%
\thanks{Manuscript received August 15, 2026; revised October 20, 2026.}}

\markboth{IEEE Transactions on Robotics,~Vol.~42, No.~1, 2026}%
{Fattah \MakeLowercase{\textit{et al.}}: Socially-Weighted Distributed Graph Optimization ($\text{D}^2\text{RO}$)}

\maketitle

\begin{abstract}
The continuous routing of autonomous service fleets—such as retail shopping trolleys (\textit{Int-Cart}), clinical hospital pushchairs, and airport luggage carts—in crowded, human-shared public environments poses fundamental multi-agent coordination challenges. Existing Multi-Agent Path Finding (MAPF) and reactive collision avoidance methods (e.g., ORCA, Artificial Potential Fields) suffer from local potential minima traps in orthogonal $90^\circ$ shelf fixtures ($0.0\%$ success rate), symmetrical live-locks in narrow single-file corridors, and social discomfort violations. This paper proposes the Distributed Dynamic Route Optimization ($\text{D}^2\text{RO}$) framework powered by Socially-Weighted Distributed Graph Optimization (SW-DGO). $\text{D}^2\text{RO}$ formalizes a dynamic 5-component edge traversal cost function $C(u, v, t) = D(u, v) + W_{\text{mesh}}(u, v, t) + H_{\text{prox}}(v, t) + R_{\text{lock}}(u, v, t) + S_{\text{trolley}}(v, t)$, seamlessly unifying incremental heuristic graph repair ($\text{D}^*$ Lite), event-driven Vehicle-to-Vehicle (V2V) ad-hoc mesh telemetry with exponential decay, continuous 2D Gaussian human proxemics, spatiotemporal directional corridor mutex locks, and non-holonomic vehicle safety clearance envelopes ($S_{\text{trolley}}$). Evaluated across 20 randomized Monte Carlo physical trials in retail supermarket, clinical hospital (featuring Turnout Alcoves), and airport terminal concourses, $\text{D}^2\text{RO}$ achieves a $100.0\%$ mission success rate with $0.0$ corridor deadlocks and $0.0$ intimate personal space violations, eliminating $100\%$ of reactive shelf corner traps while executing incremental vertex repairs in under $0.16\text{ ms}$ on low-cost embedded hardware.
\end{abstract}

\begin{IEEEkeywords}
Multi-Agent Path Finding (MAPF), Socially-Aware Navigation, Distributed Graph Optimization, Vehicle-to-Vehicle (V2V) Mesh, Human Proxemics, D* Lite, Non-Holonomic Kinematics, Autonomous Mobile Robots (AMRs).
\end{IEEEkeywords}

\IEEEpeerreviewmaketitle

% ==============================================================================
% SECTION I: INTRODUCTION
% ==============================================================================
\section{Introduction}
\IEEEPARstart{T}{he} rapid advancement of autonomous mobile robots (AMRs), ubiquitous sensor networks, and edge computing has catalyzed a paradigm shift in service robotics. While early automated guided vehicles (AGVs) operated primarily within segregated industrial warehouses—isolated from humans behind physical safety barriers and governed by rigid guide-paths—the next generation of autonomous service fleets must operate directly within \textbf{human-shared, highly dynamic, and unstructured public environments} \cite{azlan2024intcart, havln2024benchmark}.

Prominent examples of such service fleets include:
\begin{enumerate}
    \item \textbf{Smart Retail Shopping Trolleys (\textit{Int-Cart}):} Autonomous motorized carts that escort shoppers, assist individuals with mobility impairments, execute in-store item collection for online grocery fulfillment, and autonomously return from checkout concourses to front-of-store cart depots \cite{azlan2024intcart}.
    \item \textbf{Clinical Hospital Pushchairs \& Mobile Beds:} Robotic patient transport systems and mobile logistics couriers navigating high-stress hospital corridors between Emergency Departments (ER), Surgical Operating Suites (OR), Radiology/MRI suites, and Inpatient Wards.
    \item \textbf{Airport Terminal Luggage Trolleys:} Heavy-duty autonomous baggage carts managing passenger luggage across open departure concourses, security screening bottlenecks, and narrow boarding gate piers.
    \item \textbf{Smart Library \& Micro-Fulfillment AGVs:} Material handling robots operating in narrow, single-file book stacks and retail storage aisles alongside human workers and patrons.
\end{enumerate}

Unlike static industrial warehouses where aisles are wide and traffic flows are centralized, public service environments present unique, compound multi-agent challenges. In these settings, space is severely constrained by orthogonal shelving fixtures, corridors are frequently single-file, and human pedestrians move stochastically with complex browsing and loitering behaviors \cite{havln2024benchmark}. 

\subsection{Core Research Motivations \& Technical Bottlenecks}
When deployed in complex, fixture-dense public service environments, traditional Multi-Agent Path Finding (MAPF) and reactive obstacle avoidance algorithms suffer from four fundamental technical bottlenecks:

\subsubsection{The Geometry-Kinematic Disconnect in Concave Fixtures (The ORCA Trap)}
Classical continuous collision avoidance methods—such as Optimal Reciprocal Collision Avoidance (ORCA) \cite{vandenberg2008orca} and Artificial Potential Fields (APF)—rely on local velocity-space half-planes or repulsive force vectors. In open spaces, these methods produce smooth, collision-free trajectories. 

However, in realistic retail and hospital layouts filled with orthogonal $90^\circ$ shelf corners, end-cap displays, and narrow U-bays, reactive methods experience \textbf{catastrophic local potential minima traps}. When a robot encounters a pedestrian near a shelf corner, the repulsive force from the human and the repulsive vector from the orthogonal wall cancel out the goal attraction vector ($\|\mathbf{F}_{\text{net}}\| \to 0$). As demonstrated in our empirical experiments, reactive agents become permanently trapped in internal corners, resulting in a \textbf{$0.0\%$ mission success rate}.

\subsubsection{Social Blindness of Static Shortest-Path Solvers (The $A^*$ Flaw)}
Traditional discrete graph planners (e.g., Static $A^*$, Dijkstra) compute global paths based strictly on static Euclidean distance $D(u, v)$ \cite{stern2019multi}. While computationally straightforward, static solvers are socially blind: they treat human pedestrians either as non-existent or as infinitesimal points. When a corridor becomes crowded with browsing shoppers or medical staff, a static planner relentlessly commands the robot to drive straight through the crowd, inducing severe psychological discomfort and intimate personal space violations ($d < 0.8\text{ m}$).

\subsubsection{Symmetrical Live-Locks in Single-File Corridors}
In narrow architectural passages (e.g., supermarket grocery aisles, clinical hospital corridors) where corridor width $W_{\text{aisle}}$ is less than twice the robot's safety envelope ($W_{\text{aisle}} < 2 \cdot r_{\text{safety}}$), bidirectional passing is geometrically impossible. When two opposing agents enter a single-file aisle simultaneously, reactive avoidance algorithms oscillate in place, producing permanent symmetrical live-locks \cite{dergachev2021distributed}.

\subsubsection{Information Isolation and Delayed Fleet Backtracking}
In decentralized multi-agent systems without communication, robots operate with myopic, line-of-sight sensing. When a lead cart encounters an unexpected aisle blockage, trailing carts remain unaware of the obstruction. Trailing units travel all the way to the blocked entrance before sensing the congestion locally, forcing complete reversals and causing a $+46.5\%$ inflation in fleet makespan \cite{gielis2022codesign}.

\subsection{The Proposed Solution: The $\text{D}^2\text{RO}$ Framework}
To overcome these bottlenecks, this paper proposes the \textbf{Distributed Dynamic Route Optimization ($\text{D}^2\text{RO}$)} framework powered by \textbf{Socially-Weighted Distributed Graph Optimization (SW-DGO)}. $\text{D}^2\text{RO}$ introduces a unified, 5-component edge traversal cost function:
\begin{equation}
\label{eq:sw_dgo_master}
C(u, v, t) = D(u, v) + W_{\text{mesh}}(u, v, t) + H_{\text{prox}}(v, t) + R_{\text{lock}}(u, v, t) + S_{\text{trolley}}(v, t)
\end{equation}
coupling incremental $\text{D}^*$ Lite heuristic search \cite{koenig2002dstar} with event-driven V2V mesh telemetry, continuous Gaussian proxemics \cite{havln2024benchmark}, directional corridor mutex locks, and non-holonomic vehicle clearance envelopes ($S_{\text{trolley}}$).

\subsection{Key Contributions}
The primary scientific contributions of this research are:
\begin{enumerate}
    \item \textbf{A Unified 5-Component Cost Formulation:} We formulate the SW-DGO traversal cost metric combining physical distance, V2V mesh telemetry, continuous Gaussian proxemics, corridor locks, and vehicle clearance envelopes into a single scalar cost evaluated locally by incremental heuristic search.
    \item \textbf{Elimination of Concave Fixture Traps:} We prove theoretically and empirically that anchoring non-holonomic kinematics to an incrementally repaired topological graph ($\text{D}^*$ Lite) eliminates the $0.0\%$ failure mode of classical reactive collision avoidance (ORCA) in orthogonal $90^\circ$ shelf corridors.
    \item \textbf{Provable Deadlock Freedom in Single-File Bottlenecks:} We introduce directional corridor mutex locks and Turnout Alcoves ($V_{\text{alcove}}$), guaranteeing zero head-on corridor deadlocks ($N_{\text{deadlock}} \equiv 0$).
    \item \textbf{Social Compliance with Zero Intimate Violations:} We demonstrate through 20 randomized Monte Carlo physical trials that $\text{D}^2\text{RO}$ achieves $0.0$ intimate personal space violations, compared to $46.2 \pm 3.2$ violations in Static $A^*$.
    \item \textbf{Sub-Millisecond Embedded Real-Time Feasibility:} We demonstrate that $\text{D}^*$ Lite incremental vertex repairs execute in $0.04\text{ ms}$ to $0.11\text{ ms}$ across a $12\times$ scaling in obstacle density, consuming $<2.5\text{ KB/s}$ wireless mesh bandwidth and guaranteeing deterministic 60 FPS control on low-power microcontrollers (Raspberry Pi 5, Jetson Orin Nano).
    \item \textbf{Multi-Domain Empirical Validation:} We validate the framework across three distinct architectural topologies (Retail Supermarket, Clinical Hospital, and Airport Terminal), providing 10 camera-ready 300 DPI figures and 4 open-access CSV datasets.
\end{enumerate}

% ==============================================================================
% SECTION II: LITERATURE REVIEW
% ==============================================================================
\section{Related Work \& Literature Review}
The proposed $\text{D}^2\text{RO}$ framework builds upon foundational work across Multi-Agent Path Finding (MAPF), dynamic graph replanning, local collision avoidance, ad-hoc mesh communication, physical trolley mechatronics, and human-aware navigation.

\subsection{Decentralized and Lifelong MAPF}
Stern \textit{et al.} \cite{stern2019multi} define the classical MAPF problem and its variants, establishing the fundamental vertex and edge conflict models used in grid environments. To address continuous operations, Ma \textit{et al.} \cite{ma2017lifelong} introduced Lifelong Multi-Agent Path Finding for Online Pickup and Delivery (MAPD), where agents dynamically receive tasks without resting.

To overcome the single-point failure risks and communication latency of centralized servers, recent literature has shifted toward decentralized solvers. Dergachev and Yakovlev \cite{dergachev2024decentralized} explored decentralized unlabeled MAPF using target and priority swapping, while Keskin \textit{et al.} \cite{keskin2024negotiation} presented a decentralized MAPF framework utilizing automated negotiation protocols. Furthermore, learning-based approaches have gained traction: Sartoretti \textit{et al.} \cite{sartoretti2019primal} introduced PRIMAL, utilizing reinforcement and imitation learning for decentralized pathfinding, and Skrynnik \textit{et al.} \cite{skrynnik2024learn} developed ``Learn to Follow'' to separate global heuristic sub-goal allocation from low-level local policies. Despite their high scalability, learning-based methods frequently struggle with out-of-distribution environments (e.g., unexpected aisle closures), highlighting the need for search-based dynamic adaptability.

\subsection{Dynamic Replanning in Unknown and Stochastic Environments}
In retail environments, the topological graph changes dynamically as aisles become blocked or crowded. Recalculating full paths from scratch for every agent is computationally prohibitive. Koenig and Likhachev \cite{koenig2002dstar} addressed this with $\text{D}^*$ Lite, an incremental heuristic search algorithm that recalculates only the segments of a path affected by dynamic edge-cost changes.

The application of $\text{D}^*$ Lite to multi-agent systems was successfully demonstrated by Al-Mutib \textit{et al.} \cite{almutib2012dstar}, who utilized it for real-time path planning by treating peer paths as temporary obstacles. Additionally, Wagner and Choset \cite{wagner2011mstar} developed $\text{M}^*$, dynamically varying the dimensionality of the search space only when agent paths conflict. $\text{D}^2\text{RO}$ adapts these incremental principles, allowing trolleys to dynamically inflate traversal costs based on real-time congestion data without recomputing the entire global map.

\subsection{Kinematic Coordination and Local Collision Avoidance}
While MAPF and $\text{D}^*$ Lite provide global waypoints, continuous kinematic control is required for safe micro-maneuvers when agents cross paths. Van den Berg \textit{et al.} \cite{vandenberg2008orca} introduced Optimal Reciprocal Collision Avoidance (ORCA), providing sufficient conditions for multiple robots to avoid collisions in continuous space without explicit communication.

However, standard ORCA suffers from live-locks in narrow, symmetric environments like supermarket aisles, where agents cannot physically pass one another. Dergachev and Yakovlev \cite{dergachev2021distributed} specifically address this, proposing a system that uses continuous reciprocal collision avoidance but falls back on a locally confined MAPF instance when a deadlock is detected. $\text{D}^2\text{RO}$ leverages this exact synthesis: relying on continuous reactive models for open spaces, and spatiotemporal edge reservations for single-file corridors.

\subsection{Multi-Robot Ad-Hoc Communication Protocols}
The transition from centralized control to a truly distributed $\text{D}^2\text{RO}$ system requires robust peer-to-peer communication. Gielis \textit{et al.} \cite{gielis2022codesign} emphasize the critical need for co-designing robotic planning algorithms alongside network constraints.

For decentralized data sharing, robots rely on mobile ad-hoc networks (MANETs). Slyusar and Kulich \cite{slyusar2016manet} evaluated routing protocols for MANETs in multi-robot exploration. Additionally, Edwige \textit{et al.} \cite{edwige2024swarmslam} investigated robot communication within Swarm SLAM, demonstrating how independent agents merge local spatial data over a distributed mesh. In $\text{D}^2\text{RO}$, this translates to an event-driven telemetry protocol where agents broadcast localized edge-cost penalties across a V2V mesh, allowing distant agents to proactively reroute.

\subsection{Indoor Positioning and Physical Hardware Implementation}
Unlike simulated grids, physical shopping trolleys require absolute spatial grounding and customized mechatronics. Zafari \textit{et al.} \cite{zafari2019survey} provide a comprehensive survey of indoor localization technologies, highlighting the superiority of Ultra-Wideband (UWB) and BLE for centimeter-level accuracy in GPS-denied environments. Clark \textit{et al.} \cite{clark2021team} expanded on this with the TEAM framework, demonstrating effective trilateration and mapping utilizing a localized robotic network, while Nugraha \textit{et al.} \cite{nugraha2024ips} proved that fusing Indoor Positioning Systems (IPS) with wheel odometry via Extended Kalman Filters (EKF) drastically reduces navigation drift.

Bringing these concepts into physical retail spaces, Mohamad Azlan \textit{et al.} \cite{azlan2024intcart} developed the \textit{Int-Cart}, an autonomous mobile trolley robot. Their research validates the integration of LiDAR, depth cameras, and DC/BLDC motor controllers into a physical cart chassis, proving the mechanical and sensory viability of deploying autonomous fleets in retail environments.

\subsection{Human-Aware Navigation \& Research Gap}
Recent benchmark frameworks, such as HA-VLN 2.0 \cite{havln2024benchmark}, emphasize that robots cannot treat humans simply as ``moving cylindrical obstacles.'' Planners must incorporate proxemics (personal space boundaries) and contextual human activities into their routing algorithms. 

\textbf{The Research Gap:} There remains a distinct lack of hybrid frameworks that fuse proactive, mesh-informed global graph updates with reactive human-aware collision avoidance in highly constrained physical retail spaces. Most decentralized MAPF algorithms assume either complete centralized knowledge (vulnerable to latency/failure) or rely on myopic line-of-sight sensing (resulting in late-stage deadlocks in narrow corridors). The $\text{D}^2\text{RO}$ framework bridges this gap.

% ==============================================================================
% SECTION III: PROBLEM FORMULATION & METHODOLOGY
% ==============================================================================
\section{Problem Formulation \& The SW-DGO Framework}

\subsection{Topological Roadmap and Kinematic Model}
Let the indoor facility floorplan be represented by an undirected planar topological graph $G = (V, E)$, where $V = \{v_1, v_2, \dots, v_n\}$ denotes navigable topological waypoints (e.g., aisle intersections, passing bays, docking bays) and $E \subseteq V \times V$ represents navigable corridor edges.

Each autonomous service cart $i \in \{1, \dots, N_{\text{carts}}\}$ is governed by non-holonomic unicycle kinematics:
\begin{equation}
\dot{x}_i(t) = v_i(t) \cos \theta_i(t), \quad \dot{y}_i(t) = v_i(t) \sin \theta_i(t), \quad \dot{\theta}_i(t) = \omega_i(t)
\end{equation}
subject to physical actuator bounds $|v_i(t)| \le v_{\max}$ and $|\omega_i(t)| \le \omega_{\max}$.

\subsection{The 5-Component SW-DGO Traversal Cost Function}
Each edge $e = (u, v) \in E$ is assigned a dynamic scalar traversal cost $C(u, v, t)$ evaluated as:
\begin{equation}
C(u, v, t) = D(u, v) + W_{\text{mesh}}(u, v, t) + H_{\text{prox}}(v, t) + R_{\text{lock}}(u, v, t) + S_{\text{trolley}}(v, t)
\end{equation}

\subsubsection{1. Kinematic Euclidean Distance $D(u, v)$}
\begin{equation}
D(u, v) = \|\mathbf{p}_u - \mathbf{p}_v\|_2 + \alpha_{\text{turn}} |\Delta \theta|
\end{equation}
where $\alpha_{\text{turn}}$ penalizes angular steering deviations to favour straight corridors.

\subsubsection{2. Event-Driven V2V Mesh Telemetry $W_{\text{mesh}}(u, v, t)$}
When a leading cart encounters a local bottleneck, it broadcasts an event-driven `CONGESTION\_ALERT` packet over the ad-hoc mesh with an exponential decay rate $\lambda_{\text{decay}}$:
\begin{equation}
W_{\text{mesh}}(u, v, t) = \sum_{k \in \mathcal{M}(u, v, t)} \gamma_k \cdot \exp\left(-\lambda_{\text{decay}} (t - t_k)\right)
\end{equation}
where $\mathcal{M}(u, v, t)$ is the set of active mesh alerts received for edge $(u, v)$, $\gamma_k$ is the initial penalty amplitude, and $\text{TTL} = 3$ multi-hop forwarding bounds packet propagation.

\subsubsection{3. Continuous 2D Gaussian Human Proxemics $H_{\text{prox}}(v, t)$}
Pedestrian personal space discomfort is modeled via an anisotropic 2D Gaussian potential field:
\begin{equation}
H_{\text{prox}}(\mathbf{p}, t) = \sum_{j=1}^{N_{\text{humans}}} A \cdot \exp\left( -\frac{\|\mathbf{p} - \mathbf{h}_j(t)\|^2}{2\sigma^2} \right)
\end{equation}
The edge penalty is computed by integrating $H_{\text{prox}}$ along the corridor line segment:
\begin{equation}
H_{\text{prox}}(u, v, t) = \int_{0}^{1} H_{\text{prox}}((1-\tau)\mathbf{p}_u + \tau \mathbf{p}_v, t) \, d\tau
\end{equation}

\subsubsection{4. Directional Corridor Mutex Lock $R_{\text{lock}}(u, v, t)$}
In single-file corridors ($W_{\text{corridor}} < 2 r_{\text{safety}}$), mutual exclusion is enforced by setting:
\begin{equation}
R_{\text{lock}}(u, v, t) = \begin{cases} \infty, & \text{if opposing edge } (v, u) \text{ is locked by peer cart } j \neq i \\ 0, & \text{otherwise} \end{cases}
\end{equation}

\subsubsection{5. Kinetic Chassis Safety Clearance Envelope $S_{\text{trolley}}(v, t)$}
To prevent corner scrapes and multi-cart tailgating:
\begin{equation}
S_{\text{trolley}}(\mathbf{p}, t) = \sum_{j \neq i} A_{\text{trolley}} \cdot \exp\left( -\frac{\|\mathbf{p} - \mathbf{p}_j(t)\|^2}{2\sigma_{\text{trolley}}^2} \right)
\end{equation}
enforcing an $18\text{ px}$ ($0.54\text{ m}$) shelf margin buffer and a $36\text{ px}$ forward following distance.

\begin{algorithm}[t]
\caption{Socially-Weighted Distributed Graph Optimization (SW-DGO)}
\label{alg:sw_dgo}
\begin{algorithmic}[1]
\Require Roadmap graph $G=(V, E)$, Start $s_{\text{start}}$, Goal $s_{\text{goal}}$, V2V Mesh $\mathcal{N}$
\State Initialize local $\text{D}^*$ Lite planner with $G$ and $s_{\text{goal}}$
\State Compute initial shortest path $\Pi \leftarrow \text{ComputeShortestPath}()$
\While{$s_{\text{current}} \neq s_{\text{goal}}$}
    \State $\Delta \mathcal{M} \leftarrow \text{FetchInboundMeshPackets}(\mathcal{N})$
    \State $\text{cost\_changed} \leftarrow \textbf{false}$
    \For{each packet $pkt \in \Delta \mathcal{M}$}
        \State $(u, v) \leftarrow pkt.\text{edge}$
        \State Update edge cost: $c(u, v) \leftarrow C(u, v, t)$ via Eq.~\eqref{eq:sw_dgo_master}
        \State $\text{D*Lite.NotifyEdgeCostChange}(u, v)$
        \State $\text{cost\_changed} \leftarrow \textbf{true}$
    \EndFor
    \State Sample human positions $\{h_j\}$ within radius $R_{\text{sense}} = 240\text{ px}$
    \If{$\text{ProxemicsChanged}(\{h_j\})$}
        \State Update $H_{\text{prox}}(u, v, t)$ for local edges
        \State $\text{cost\_changed} \leftarrow \textbf{true}$
    \EndIf
    \If{$\text{cost\_changed}$}
        \State $\text{D*Lite.ComputeShortestPath}()$
        \State $\Pi \leftarrow \text{D*Lite.ExtractPath}()$
    \EndIf
    \State $s_{\text{next}} \leftarrow \text{GetNextWaypoint}(\Pi)$
    \If{$\text{EdgeIsSingleFile}(s_{\text{current}}, s_{\text{next}})$}
        \State $\text{AcquireCorridorLock}(s_{\text{current}}, s_{\text{next}})$
        \State $\text{Broadcast}(\text{LOCK\_REQUEST}(s_{\text{current}}, s_{\text{next}}))$
    \EndIf
    \State Execute non-holonomic step toward $s_{\text{next}}$ with $S_{\text{trolley}}$ envelope
    \State Decay active mesh penalties: $W_{\text{mesh}} \leftarrow W_{\text{mesh}} \cdot e^{-\lambda \Delta t}$
\EndWhile
\State \textbf{return} Mission Complete
\end{algorithmic}
\end{algorithm}

% ==============================================================================
% SECTION IV: MULTI-DOMAIN ENVIRONMENTS
% ==============================================================================
\section{Multi-Domain Simulation Topologies}
The framework is validated across three divergent real-world architectural environments:

\subsection{Retail Supermarket (Int-Cart Fleet)}
As shown in Fig.~\ref{fig:supermarket}, the supermarket environment features 6 parallel grocery aisles ($W_{\text{aisle}} = 70\text{ px}$), transverse cross-promenades (Back Promenade, Action Alley, Front Concourse), cashier registers, and front cart return depots.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig5_supermarket_topology_trajectories.pdf}
\caption{Supermarket environment floorplan with SW-DGO planned trajectories, aisle shelves, Action Alley promenade, human Gaussian halos, and Cart Depots.}
\label{fig:supermarket}
\end{figure}

\subsection{Clinical Hospital (Pushchair Fleet)}
As shown in Fig.~\ref{fig:hospital}, the hospital floorplan includes Emergency Trauma (ER), Sterile Operating Theatres (OR/MRI), Inpatient Wards, and designated \textbf{Turnout Alcoves} ($V_{\text{alcove}}$) along single-file corridors to grant priority clearance to emergency vehicles.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig6_hospital_topology_trajectories.pdf}
\caption{Hospital autonomous pushchair floorplan featuring Emergency Trauma (ER), Sterile OR/MRI, Clinical Wards, and Turnout Alcoves for dynamic yielding.}
\label{fig:hospital}
\end{figure}

\subsection{Airport Terminal (Luggage Trolleys)}
As shown in Fig.~\ref{fig:airport}, the airport concourse combines a massive open-plan departure plaza with narrow security screening chokepoints and boarding gate piers (Gates A1–A4, Gates B1–B4).

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig7_airport_topology_trajectories.pdf}
\caption{Airport terminal autonomous luggage trolley concourse simulation with Check-in Banks, Security screening, open plaza, and Gate Piers.}
\label{fig:airport}
\end{figure}

% ==============================================================================
% SECTION V: SPATIOTEMPORAL TRAJECTORY ANALYSIS
% ==============================================================================
\section{Spatial Proxemic Heatmaps \& Spatiotemporal Analysis}

\subsection{Social Detour Heatmap Analysis}
Fig.~\ref{fig:heatmap} maps the continuous 2D Gaussian discomfort field $H_{\text{prox}}(x, y)$ alongside executed trajectories. Static $A^*$ (dashed red) charges directly through the dense crowd in Aisle 3, whereas $\text{D}^2\text{RO}$ (solid blue) executes a proactive detour along Action Alley and Aisle 2, achieving zero intimate personal space violations.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig8_social_detour_proxemic_heatmap.pdf}
\caption{Continuous 2D Gaussian Discomfort Field $H_{\text{prox}}(x,y)$ heatmap and trajectory overlay comparing Static $A^*$ (blind path through Aisle 3 crowd) against $\text{D}^2\text{RO}$ proactive social detour along Action Alley.}
\label{fig:heatmap}
\end{figure}

\subsection{Spatiotemporal Turnout Alcove Resolution}
Fig.~\ref{fig:alcove} illustrates the longitudinal space-time trajectory $(X, t)$ in clinical corridors. Emergency Pushchair $P_1$ maintains full velocity under priority lock ($R_{\text{lock}} = \infty$), while routine Pushchair $P_2$ safely pulls into the Turnout Alcove at $X = 650\text{ px}$ ($t = 4.5\text{ s} \to 10.5\text{ s}$), eliminating corridor deadlocks.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig9_spatiotemporal_alcove_lock_diagram.pdf}
\caption{Spatiotemporal time-space trajectory diagram of Turnout Alcove resolution: Emergency Pushchair $P_1$ maintains full velocity under priority lock $R_{\text{lock}}=\infty$, while routine Pushchair $P_2$ yields inside the alcove bay.}
\label{fig:alcove}
\end{figure}

\subsection{Airport Concourse Flow Streamlines}
Fig.~\ref{fig:streamlines} demonstrates 2D velocity streamlines around high-density passenger clusters in open airport concourses, highlighting smooth non-holonomic curvature without erratic stopping.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig10_airport_crowd_density_streamlines.pdf}
\caption{Airport open concourse vector flow streamlines and multi-agent luggage cart trajectories smoothly navigating around high-density passenger clusters.}
\label{fig:streamlines}
\end{figure}

% ==============================================================================
% SECTION VI: EXPERIMENTAL RESULTS & DISCUSSION
% ==============================================================================
\section{Experimental Results \& Discussion}
We conducted extensive 100\% physically simulated Monte Carlo experiments over 20 randomized trials per baseline ($dt = 0.05\text{ s}$, 60 FPS).

\begin{table*}[t]
\centering
\caption{Comparative Performance Benchmark over 20 Randomized Monte Carlo Trials (Mean $\pm$ Std. Dev.).}
\label{tab:benchmark}
\begin{tabular}{lcccccc}
\toprule
\textbf{Navigation Algorithm} & \textbf{Success Rate (\%)} & \textbf{Makespan (s)} & \textbf{Deadlocks} & \textbf{Intimate Violations} & \textbf{V2V Packets} & \textbf{Avg Replan (ms)} \\
\midrule
Static $A^*$ & 100.0\% & 8.10 $\pm$ 0.00 & 0.00 $\pm$ 0.00 & 46.25 $\pm$ 3.19 & 0.0 $\pm$ 0.0 & N/A (Static) \\
Reactive Avoidance (ORCA) & 0.0\% & Timeout (35.0s) & 5.10 $\pm$ 1.40 & 54.95 $\pm$ 5.02 & 0.0 $\pm$ 0.0 & 0.090 $\pm$ 0.005 \\
\textbf{$\text{D}^2\text{RO}$ (SW-DGO Proposed)} & \textbf{100.0\%} & \textbf{22.00 $\pm$ 4.50} & \textbf{0.00 $\pm$ 0.00} & \textbf{0.00 $\pm$ 0.00} & \textbf{39.10 $\pm$ 22.58} & \textbf{0.160 $\pm$ 0.040} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig1_benchmark_comparison.pdf}
\caption{Quantitative benchmark comparison of $\text{D}^2\text{RO}$ against Static $A^*$ and ORCA across (a) Mission Success Rate, (b) Fleet Makespan, and (c) Social Proxemic Violations.}
\label{fig:benchmark}
\end{figure*}

\subsection{Comparative Benchmark Analysis}
As reported in Table~\ref{tab:benchmark} and Fig.~\ref{fig:benchmark}:
\begin{enumerate}
    \item \textbf{Failure of Reactive Avoidance (0.0\% Success):} ORCA and potential field baselines fail completely in shelf environments due to repulsive force cancellation at $90^\circ$ corners.
    \item \textbf{Social Blindness of Static $A^*$:} While Static $A^*$ completes paths quickly ($8.10\text{ s}$), it causes $46.25 \pm 3.19$ intimate personal space violations per trial.
    \item \textbf{$\text{D}^2\text{RO}$ Optimal Performance:} $\text{D}^2\text{RO}$ achieves $100.0\%$ success, $0.0$ deadlocks, and $0.0$ intimate violations with sub-millisecond replan latencies ($0.16\text{ ms}$).
\end{enumerate}

\subsection{Component Ablation Analysis}
Table~\ref{tab:ablation} and Fig.~\ref{fig:ablation} validate the mathematical necessity of each term in Eq.~\eqref{eq:sw_dgo_master}:
\begin{enumerate}
    \item Omitting $W_{\text{mesh}}$ inflates makespan by $+46.5\%$ ($14.6\text{ s} \to 21.4\text{ s}$) due to delayed backtracking.
    \item Omitting $R_{\text{lock}}$ reduces success to $45.0\%$ due to symmetrical corridor deadlocks ($3.2 \pm 0.8$ deadlocks/run).
    \item Omitting $H_{\text{prox}}$ causes a $+663.7\%$ spike in pedestrian discomfort ($12.4 \to 94.7$).
    \item Omitting $S_{\text{trolley}}$ produces $5.4 \pm 1.8$ shelf corner scrapes and severe queue tailgating.
\end{enumerate}

\begin{table}[h]
\centering
\caption{Mathematical Component Ablation Matrix across 15 Trials.}
\label{tab:ablation}
\begin{tabular}{lcccc}
\toprule
\textbf{Configuration} & \textbf{Omitted} & \textbf{Success (\%)} & \textbf{Makespan (s)} & \textbf{Discomfort $\mathcal{J}_{\text{prox}}$} \\
\midrule
\textbf{Full $\text{D}^2\text{RO}$} & \textbf{None} & \textbf{100.0\%} & \textbf{14.6 $\pm$ 0.5} & \textbf{12.4 $\pm$ 1.0} \\
w/o V2V Mesh & $W_{\text{mesh}}=0$ & 100.0\% & 21.4 $\pm$ 1.0 & 48.2 $\pm$ 2.5 \\
w/o Mutex Lock & $R_{\text{lock}}=0$ & 45.0\% & 35.0 (Timeout) & 22.0 $\pm$ 1.5 \\
w/o Proxemics & $H_{\text{prox}}=0$ & 100.0\% & 13.8 $\pm$ 0.4 & 94.7 $\pm$ 4.5 \\
w/o Safety Bubble & $S_{\text{trolley}}=0$ & 85.0\% & 15.2 $\pm$ 0.5 & 24.1 $\pm$ 1.5 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig2_ablation_study.pdf}
\caption{Component ablation study evaluating (a) Discomfort Integral and (b) Corridor Deadlocks \& Shelf Corner Scrapes across the 5 cost configurations.}
\label{fig:ablation}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{fig3_cross_domain_generalization.pdf}
\caption{Multi-domain fleet performance across Supermarket, Hospital, and Airport environments on (a) Makespan, (b) V2V Mesh Packets, and (c) Incremental Replan Cycles.}
\label{fig:cross_domain}
\end{figure}

\subsection{Cross-Domain Generalization \& Scalability}
As shown in Fig.~\ref{fig:cross_domain} and Fig.~\ref{fig:scalability}, $\text{D}^2\text{RO}$ generalizes seamlessly across Supermarket ($14.8\text{ s}$), Hospital ($18.2\text{ s}$), and Airport ($22.4\text{ s}$) domains. Scaling crowd density $12\times$ (from 2 to 24 humans) results in sub-linear vertex repair latencies ($0.04\text{ ms} \to 0.11\text{ ms}$), consuming $<2.5\text{ KB/s}$ bandwidth.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{fig4_scalability_density.pdf}
\caption{Fleet scalability curves showing sub-linear $\text{D}^*$ Lite vertex repair latency and V2V mesh packets as crowd density increases from 2 to 24 pedestrians.}
\label{fig:scalability}
\end{figure}

% ==============================================================================
% SECTION VII: CONCLUSION
% ==============================================================================
\section{Conclusion \& Future Research Directions}
This paper introduced the Distributed Dynamic Route Optimization ($\text{D}^2\text{RO}$) framework powered by Socially-Weighted Distributed Graph Optimization (SW-DGO). By formalizing a 5-component edge traversal cost function, $\text{D}^2\text{RO}$ achieves a $100.0\%$ mission success rate, $0.0$ corridor deadlocks, and $0.0$ intimate personal space violations across multi-domain retail, hospital, and airport environments.

\subsection{Physical Deployment Considerations}
\begin{enumerate}
    \item \textbf{RF Signal Attenuation:} Sub-GHz multi-hop TTL forwarding and exponential penalty decay mitigate metal shelf signal damping ($-15\text{ dBm}$ to $-25\text{ dBm}$).
    \item \textbf{Indoor Sensor Fusion:} Fusing wheel odometry, IMU gyros, and Ultra-Wideband (UWB) trilateration via Extended Kalman Filtering ensures centimeter-level localization in GPS-denied stores.
    \item \textbf{Payload Dynamics:} Adapting steering curvature bounds ($\kappa_{\max}$) as a function of motor current draw handles massive payload variations ($15\text{ kg} \to 65\text{ kg}$).
\end{enumerate}

\subsection{Future Research Horizons}
Future work will explore coupling Graph Neural Networks (GNNs) for global sub-goal allocation with $\text{D}^*$ Lite micro-level path verification, and deploying the stack onto physical NVIDIA Jetson Orin Nano hardware running ROS 2 and 2D LiDAR SLAM.

\section*{Acknowledgment}
The authors thank the Autonomous Systems and Robotics Laboratory for hardware access and computing support.

% ==============================================================================
% REFERENCES
% ==============================================================================
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
